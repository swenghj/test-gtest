// simple functions 

#ifndef APPS_H
#define APPS_H

#include <math.h>
#include <string>
using std::string;

string append_strings(const string str1, const string str2);
int add_two_integers(const int a, const int b); 
double square_root(const double a);

#endif